<table class="table">
  <% @mentionedWith.each do |id| %>
    <tr>
      <td><%= id.first %></td>
      <td><a href="<%= "/id/#{id.first}/#{id.second}" %>"><%= id.second %></a></td>
      <td>
        <button class="btn btn-default disabled"><span class="glyphicon glyphicon-ok"></span> Agree 9</button>
        <button class="btn btn-default disabled"><span class="glyphicon glyphicon-remove"></span> Disagree 1</button>
      </td>
      <td>
      <div class="progress" style="min-width: 50px">
        <div class="progress-bar" role="progressbar" aria-valuenow="90" aria-valuemin="0" aria-valuemax="100" style="width: 90%;">
          90%
        </div>
      </div>

      </td>
    </tr>
  <% end %>
    <tr>
      <td><button class="btn btn-default disabled"><span class="glyphicon glyphicon-plus"></span> Add connection</button></td>
    </tr>
</table>