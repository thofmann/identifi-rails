<% if current_user %>
<div id="user-box" class="panel panel-default">
  <div class="panel-body">
    <h4><a href="<%= "/id/#{current_user.provider}/#{current_user.uid}"  %>"><%= current_user.name %></a></h4>
    <p>
    <% if @userOverview[:score] != "-" %>
    <strong><%= @userOverview[:score] %> %</strong> positive ratings
    <% else %>
    Not rated by anyone
    <% end %>
    </p>
    <p><strong><%= @userOverview["trustMapSize"] %></strong> identifiers in your trust map</p>
  </div>
</div>
<% end %>
